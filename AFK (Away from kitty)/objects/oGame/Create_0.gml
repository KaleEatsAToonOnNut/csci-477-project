depth = 100;
score = 0;
arrow1 = 1;
arrow2 = 1;
arrow3 = 1;
arrow4 = 1;
combo =0;
comboAlpha =  0;
alarm[0] = room_speed/5;

/*lines = 0;
pos = 0;

//var sm_file = get_open_filename("MusicGame2.wav|*.sm;*.txt", "")	
var sm_file = working_directory + "MiniGame2.wav.sm"
var file = file_text_open_read(sm_file);

if (file == -1)
{
	show_message("Couldn't find the file");
	exit;
}

while(!file_text_eof(file)) {
	file_text_readln(file);
	lines++;
	
}

file_text_close(file);

file_ = file_text_open_read(sm_file);
title = file_text_readln(file_);
artist = file_text_readln(file_);

repeat(8) file_text_readln(file_);

for (var i = 0; i<lines; i++ ) {
	scr_name[i] = file_text_read_string(file_);
	if (scr_name[i] = ",") scr_name[i] = "0000";
	file_text_readln(file_);
	
}
file_text_close(file_)*/

